package org.ssc.model.logic;

import org.ssc.model.Block;

public class While extends Block {
    public While(){
        this.blockName = "While";
        this.addConnection(null);
        this.addConnection(null);
        this.addConnection(null);
    }
}
